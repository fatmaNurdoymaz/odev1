using System.Globalization;

namespace Gazi.DizilerAppsube1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // int[] numbers = new int[3];     // int[] dizi demek 
            //numbers[0] = 1;                 // değerler heap bölesinde tutulur.
            //numbers[1] = 2;                 // numbers olana referans dedi.
            //numbers[2] = 3;                 // karenin içindeki sayıya index diyoruz 0 dan başlar.
            // Console.WriteLine(numbers[0]);                                // referans :heap bölgesindeki değerlerin adreslerini tutarlar.
            //int[] numbers = { 10, 20, 30 };

            //Array.Sort(numbers);
            //Array.Reverse(numbers);// büyükten küçüğe sırala tersi de olabilir


            //for (int i = 0; i <numbers.Length; i++)                     // dizi=Array
            //{
            //    Console.WriteLine(numbers[i]);           
            //}
            // Kaç adet isim sıralamak istiyorsunuz?3
            //1. ismi giriniz Ali
            //2.ismi giriniz Veli
            // 3.isim giriniz deli
            //Sıralanmış isimler
            //1-Ali
            //2Veli
            //3-Deli

          //  Console.WriteLine("How many names do you want sort?");
          //byte count=byte.Parse(Console.ReadLine());
          //  string [] names = new string[count];

          //  for (int i = 0; i < count; i++)
          //  {
          //      Console.WriteLine($"Enter{i+1}.Name:");
          //      names[i]= Console.ReadLine();
          //  }
          //  Array.Sort(names);
          //  Console.WriteLine("Sorted names:");

          //  for (int i =0; i < count; i++)
          //  {
          //      Console.WriteLine($"{i + 1}-{names[i]}");

          //  }
          






        }
    }
}
